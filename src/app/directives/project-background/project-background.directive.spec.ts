import { ProjectBackgroundDirective } from './project-background.directive';

describe('ProjectBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new ProjectBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
